{
  "name": "wubzy-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "core-js": "^3.9.1",
    "gh-pages": "^3.2.3",
    "nuxt": "^2.15.3",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "@nuxt/types": "~2.15.0"
  }
}
